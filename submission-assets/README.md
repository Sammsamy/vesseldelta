# VesselDelta submission gallery

These images were captured from one verified local browser session on July 21, 2026 after the complete guided journey passed at the desktop viewport and the single-column layout was inspected at `390 × 844`. Whenever a screenshot displays comparison ratios, both fields had finished their accelerated warm-up and passed the instrument’s numerical gate. The interface intentionally still says `FIELD EVOLVING`; these are release assets, not convergence or scientific evidence. Upload only after the final deployed build is visually compared against them.

Use `thumbnail-1200x800.jpg` for Devpost’s recommended `3:2` project thumbnail. Use `00-cover-1280x720.jpg` as the 16:9 gallery cover and YouTube-thumbnail fallback.

## Recommended order and captions

### 1. `00-cover-1280x720.jpg`

**Caption:** Begin with a prediction, not a canned animation. The default guided lab surrounds the live `160 × 70` D2Q9 grid with an illustrative 3D cutaway; the RBC-shaped objects are massless visual guides.

Use as the project cover and video-thumbnail fallback.

### 2. `01-guided-gated-reveal-1280x720.jpg`

**Caption:** After withholding ratios during recomputation, VesselDelta reveals the modeled throat jet and normalized axial near-wall gradient proxy only when both fields pass the numerical gate—not clinical wall shear stress.

### 3. `02-pressure-separation-1280x720.jpg`

**Caption:** Higher illustrative pressure changes a separate pressure–radius circumferential-tension index while CFD flow drive remains unchanged. The ratio is not mm Hg, hoop stress, or patient risk.

### 4. `03-rupture-boundary.jpg`

**Caption:** VesselDelta refuses rupture prediction because this rigid-wall model lacks measured wall thickness, tissue strength, growth history, and fluid–structure coupling.

### 5. `04-live-verification-1280x720.jpg`

**Caption:** Inspect the running model: main instrument-loop rate, low-Mach envelope, fitted Poiseuille-profile shape, density stability, finite-value guard count, and both synchronized `160 × 70` fields.

## Upload boundary

Before upload, compare each image with the public deployment at the same viewport. Replace any stale screenshot if the released UI, wording, or numerical gate differs. Do not crop away the `2D D2Q9 CFD · 3D cutaway · Illustrative` receipt from the cover. Do not use the screenshots themselves as validation evidence; the reproducible benchmark and live verification drawer are the evidence paths.
